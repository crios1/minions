# CLI Internals

CLI-related APIs for embedding or extending `GruShell`.

```{autosummary}
:toctree: _generated
:recursive:

minions._internal._domain.gru_shell
```

`GruShell` is experimental; expect breaking changes while the CLI matures.
